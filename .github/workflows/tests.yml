name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.10', '3.11', '3.12']
    
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: Install dependencies
      run: pip install -r requirements.txt
    
    - name: Run linter
      run: pip install pylint && pylint check_judol.py --disable=C0111,R0913,W0613,C0209
    
    - name: Check syntax
      run: python -m py_compile check_judol.py
    
    - name: Test imports
      run: python -c "from check_judol import main, parse_sitemap, make_session, fetch_url; print('All imports successful')"
